// Teachers use the same dashboard shell while role checks keep admin-only
// account provisioning out of their workspace.
export { AdminDashboard as TeacherDashboard } from '../admin/AdminDashboard';
